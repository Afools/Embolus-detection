# 运行环境
CUDA 10.1
python 3.7.16

# 使用方法
```
    python .\detect.py
        --save_folder=='default: save_folder' 
        --slide_path=='slide_path' 
        --model_path=='default: models/FPNrcnn.pth'
```
检测结果存放在save_folder中.